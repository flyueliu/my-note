Unicode字符:
    1.python3从str对象中获取的元素是Unicode字符。相当于python2总unicode中获取元素
    2.python2中获取的是原始字节序列，
   
码位：字符的标识。unicode标准中以4-6个十六进制数字表示，而且加前缀U+.
编码：字符的具体表述取决于所用的编码。
    1.编码是在码位和字节序列之间转换 时用的算法。
    2.把码位转换为字节序列的过程是 编码。把字节序列转换为码位的过程是解码
    
字节概要：
   



 